'use strict';

/**
 * neighborhood-guide service
 */

const { createCoreService } = require('@strapi/strapi').factories;

module.exports = createCoreService('api::neighborhood-guide.neighborhood-guide');
