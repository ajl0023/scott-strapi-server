'use strict';

/**
 * global-exposure controller
 */

const { createCoreController } = require('@strapi/strapi').factories;

module.exports = createCoreController('api::global-exposure.global-exposure');
