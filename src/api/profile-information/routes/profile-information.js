'use strict';

/**
 * profile-information router
 */

const { createCoreRouter } = require('@strapi/strapi').factories;

module.exports = createCoreRouter('api::profile-information.profile-information');
