'use strict';

/**
 * profile-information service
 */

const { createCoreService } = require('@strapi/strapi').factories;

module.exports = createCoreService('api::profile-information.profile-information');
